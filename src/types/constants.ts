export type Colors = {
  primary: string;
  accent: string;
  text: string;
  highlight: string;
  black: string;
  white: string;
}

export type Theme = {
  colors: Colors;
  font: string;
  wallpaper: string;
}